services:
  jde-e1-ibmi:
    image: "${KERAUN_JDE_IMAGE:?digest-pinned image required}"
    environment:
      POSTGRES_DB: keraun_jde
      POSTGRES_USER: keraun
      POSTGRES_PASSWORD: synthetic-only-admin
    networks:
      cartridge-internal:
        ipv4_address: 172.28.0.11
    volumes: ["jde-data:/var/lib/postgresql/data"]
    restart: unless-stopped
  dynamics-ax:
    image: "${KERAUN_AX_IMAGE:?digest-pinned image required}"
    environment:
      POSTGRES_DB: keraun_ax
      POSTGRES_USER: keraun
      POSTGRES_PASSWORD: synthetic-only-admin
    networks:
      cartridge-internal:
        ipv4_address: 172.28.0.12
    volumes: ["ax-data:/var/lib/postgresql/data"]
    restart: unless-stopped
  oracle-ebs-19c:
    image: "${KERAUN_EBS_IMAGE:?digest-pinned image required}"
    environment:
      POSTGRES_DB: keraun_ebs
      POSTGRES_USER: keraun
      POSTGRES_PASSWORD: synthetic-only-admin
    networks:
      cartridge-internal:
        ipv4_address: 172.28.0.13
    volumes: ["ebs-data:/var/lib/postgresql/data"]
    restart: unless-stopped
  evidence-runner:
    image: "${KERAUN_RUNNER_IMAGE:?digest-pinned image required}"
    runtime: runsc
    depends_on: [jde-e1-ibmi, dynamics-ax, oracle-ebs-19c]
    networks: [cartridge-internal]
    environment:
      KERAUN_JDE_HOST: 172.28.0.11
      KERAUN_AX_HOST: 172.28.0.12
      KERAUN_EBS_HOST: 172.28.0.13
    read_only: true
    tmpfs: ["/tmp"]
    security_opt: ["no-new-privileges:true"]
    cap_drop: [ALL]
    pids_limit: 64
    mem_limit: 256m
    cpus: 0.50
    profiles: [evidence]
networks:
  cartridge-internal:
    internal: true
    ipam:
      config:
        - subnet: 172.28.0.0/24
volumes:
  jde-data:
  ax-data:
  ebs-data:
