# Local credentials and environment overrides
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
venv/
.venv/

# Node / frontend build output
node_modules/
dist/
coverage/

# Local orchestration and agent runtime state
.orchestrator/
.task-context/
worktrees/
*.log

# Editor-local project state
.nova/
.DS_Store

# Go build output. The Docker build compiles its own Linux binary;
# this is a local artifact and must never be committed.
studio-backend/mission-control
